# Box Mount binaries + upstream docs are PRIVATE / CONFIDENTIAL (Box preview).
# Obtain them from Box and stage them under box-mount/ locally before building
# (box-mount/linux/amd64/box-mount, box-mount/linux/arm64/box-mount). They must
# NEVER be committed to this public repo, so the whole directory is ignored.
box-mount/
