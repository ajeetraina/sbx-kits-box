# Box AgentMount binaries + upstream docs are PRIVATE / CONFIDENTIAL (Box preview).
# Obtain them from Box and drop them into agentmount/<arch>/ locally before
# building. They must NEVER be committed to this public repo.
agentmount/linux/agent-mount
agentmount/linux-arm64/
agentmount/mac/agent-mount

# Upstream AgentMount CLI doc, marked "Box Confidential" — keep local only.
agentmount/README.md
