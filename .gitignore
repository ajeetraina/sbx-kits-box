# Box Mount binaries + upstream docs are PRIVATE / CONFIDENTIAL (Box preview).
# Obtain them from Box and drop them into box-mount/<arch>/ locally before
# building. They must NEVER be committed to this public repo.
box-mount/linux/box-mount
box-mount/linux-arm64/
box-mount/mac/box-mount

# Upstream Box Mount CLI doc, marked "Box Confidential" — keep local only.
box-mount/README.md
